# 05-Basic-Queries
Basic Queries

These are the foundation of SQL.

Topics include:

SELECT
WHERE
DISTINCT
ORDER BY
TOP / LIMIT
BETWEEN
IN
LIKE
IS NULL
ALIAS

Example

SELECT Name, Salary
FROM Employee
WHERE Salary > 50000
ORDER BY Salary DESC;

Skills learned

Filtering
Sorting
Selecting columns
Pattern matching